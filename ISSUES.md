# Issues — egl-util-php

The GitHub issue tracker mirrored as a checkbox-driven list, **newest first** (descending
issue number). Companion to [`ROADMAP.md`](ROADMAP.md), same conventions:

- **New issue ⇒ prepend one bullet at the top** of the list; never reorder, never renumber.
- **Issue closed ⇒ flip its checkbox** (`- [ ]` → `- [x]`) in the PR that closes it, keeping
  the row in place.
- Every bullet carries the advisory **route** in `ROADMAP.md`'s vocabulary —
  `route: <tier> / <effort>` — resolved through the same dated catalog
  ([ROADMAP.md § Model & effort routing](ROADMAP.md#model--effort-routing-advisory); as of
  2026-07-27: `fast` = Sonnet 5 · `standard` = Opus 5 · `frontier-reasoning` = Fable 5;
  efforts `low` · `medium` · `high` · `extra` · `max`). The route *recommends*; the human
  keeps final model authority.

Seeded 2026-08-09 from the seven-seat release review board held on `v1.0.0` (verdict:
**approved with conditions**, average 7.7/10). Each issue names its originating seat(s)
and evidence; issues mirroring a `ROADMAP.md` M13 item cross-reference it rather than
replacing it.

## Issues (newest → oldest)

- [ ] [#208](https://github.com/danielPoloWork/egl-util-php/issues/208) **feat: Csv::readAssoc() — header-mapped rows completing the CSV → DTO intake pipeline — M15 candidate** — filed 2026-08-28 (second review pass, maintainer-directed), and it is the one genuine gap the re-read that closed #195 found: `Csv::read()` yields positional lists, so every consumer of a *headed* CSV re-implements the first-row-as-keys loop and forgets both failure modes — a duplicate header column silently collapses two columns into one, and `array_combine` on a ragged row throws naming nothing. Duplicate headers, ragged rows and empty header cells all refused with line/column named (FR-29's enforced pairing, mirrored to the read side); still a Generator; completes `readAssoc()` → Validator (#189) → `fromArray()` — route: fast / medium
- [x] [#207](https://github.com/danielPoloWork/egl-util-php/issues/207) **feat: Str parsing batch — before/after/between, normalizeEol, codepoint-safe length/substr — M15 candidate** — the estate's most hand-rolled string arithmetic (`before`/`after`/`between` return `?string`, `null` on a miss — never the subject unchanged, which is a silent guess); `normalizeEol()` for a defect class this repository's own tooling has hit twice; and one public home for the `preg //u` codepoint mechanism ADR-0019 already chose and the Mail group already hand-rolled once — invalid UTF-8 refused, `transcode()`'s stance. No ADR expected (2.2/2.4 precedent). **Delivered 2026-08-28** as roadmap item 15.2, spec **r31** (FR-57); 3427 tests (+76). The `before`/`after` reconstruction property holds by construction (both slice the one `strpos()` call's position). Local verification caught and fixed a real order-dependent bug in `containsAny()`'s empty-needle refusal before it reached CI — route: fast / medium *(raised from route_advice.py's fast/low floor; session model Sonnet 5 — matched)*
- [ ] [#206](https://github.com/danielPoloWork/egl-util-php/issues/206) **feat: Support\Archive — zip-slip-safe, bomb-bounded ZIP extraction over Path — M15 candidate** — ZIP intake is two named attack classes in one convenience call: zip-slip (entry names walking out of the destination) and zip bombs (declared sizes lying). Every entry name passes #191's `Path::join()` before a byte is written; the extraction **budget is a required parameter** (total/per-entry bytes, entry count, ratio ceiling — an unbounded signature IS the vulnerability, so none exists); sizes measured while inflating, never trusted from the attacker-controlled header (ADR-0054's reasoning, different header); refusal removes what was already extracted. `ext-zip` in suggest, ADR-0021's refusal posture; sequenced after #191 — route: frontier-reasoning / extra
- [ ] [#205](https://github.com/danielPoloWork/egl-util-php/issues/205) **feat: NDJSON — streaming JSON Lines read/write beside Json — M15 candidate** — the bulk-interchange format (log shipping, bulk APIs, pipeline handoff) consumers hand-roll by buffering `file()` — defeating the format's whole point — and losing the failing line number. `Json::readLines()` (Generator, constant memory, malformed line refused **naming the 1-based line**, blank lines skipped per `Csv::read()`'s phantom-row reasoning) + `Json::writeLines()` over `File::writeStream()` (atomicity inherited, LF only); the trailing-partial-line policy is the ADR's stated decision. With every codec meeting at arrays/DTOs, `Json::writeLines($p, Csv::read($csv))` is a streaming converter in one line — composition is the converter, no bespoke converter class — route: frontier-reasoning / extra
- [ ] [#204](https://github.com/danielPoloWork/egl-util-php/issues/204) **feat: safe unserialize — Serialized::decode with a default-empty class allowlist, object injection refused — M15 candidate** — legacy estates keep `serialize()` output in sessions, DB columns and caches, and `unserialize()` on any of it is the PHP object-injection primitive. PHP's own mitigation silently degrades objects to `__PHP_Incomplete_Class` (exploding later, far from the boundary) — this refuses instead, naming the class; scalars/arrays-only by default, explicit `class-string` allowlist opt-in (Hmac's allowlist stance); no `encode()` counterpart, the docblock says to migrate off the format; the honest limit stated (an allowlisted gadget's `__wakeup` still runs — the allowlist bounds which gadgets are reachable, it cannot make one safe) — route: frontier-reasoning / extra
- [ ] [#203](https://github.com/danielPoloWork/egl-util-php/issues/203) **feat: Support\Ini — typed INI decode that refuses what parse_ini silently mangles — M15 candidate** — `parse_ini_string()` fails by returning `false` plus a warning, coerces reserved words by scanner mode, loses keys with unquoted special characters, and last-wins on duplicates; legacy estates are full of the format. Typed exception on failure, scanner policy stated beside `Env::get()`'s coercion table so the two never disagree about `"true"`, duplicate keys refused naming the key, file variant through `File::read()` (no second I/O path). Env/Json's wrapper shape, third application — route: fast / medium
- [ ] [#202](https://github.com/danielPoloWork/egl-util-php/issues/202) **feat: Support\Yaml — safe YAML decode/encode behind optional symfony/yaml (ADR-0021 posture) — M15 candidate** — the config lingua franca, and raw `symfony/yaml` exposes flags (`PARSE_OBJECT`, custom tags, constant resolution) one copied snippet from object injection. One safe door: flags are class constants not parameters, absent package throws naming the requirement (ADR-0021's probe-verified posture, reused verbatim), own deptrac layer only Support may reach, native ParseException wrapped typed; encode refuses objects (DTOs go through #187's `toArray()` first). Also records the conversion architecture: one canonical shape (arrays/DTOs), N codecs — conversion is composition, not a converter class — route: frontier-reasoning / extra
- [ ] [#201](https://github.com/danielPoloWork/egl-util-php/issues/201) **feat: Support\Dates + DateRange — date parsing that refuses rollover, explicit-timezone boundaries, range values — M15 candidate** — the library has a clock seam and no date tooling, and PHP's native surface silently corrupts at exactly the boundary this library guards everywhere else: `createFromFormat('Y-m-d', '2026-02-31')` returns **March 3rd**, no error, and prefix matches accept trailing garbage. Strict `fromFormat()`/`fromIso8601()` (rollover, warnings and garbage all refused, FR-47's refuse-never-clamp for dates), DST-safe day/month boundaries with explicit timezones, and a readonly `DateRange` — **half-open `[start, end)`** so adjacent billing periods neither overlap nor gap — with `contains`/`overlaps`/`iterateBy`. Nothing reads system time (FR-45's rule); no Carbon sugar, no holiday calendars, stated — route: frontier-reasoning / extra
- [ ] [#200](https://github.com/danielPoloWork/egl-util-php/issues/200) **feat: Collection<T> operations — sortBy, groupBy, keyBy, chunk, slice family under PHPStan-max generics — M15 candidate** — map/filter/reduce proved the pattern but real code calls `toArray()` and abandons the type at the first non-trivial operation. Additive read-only widening that keeps `@template-covariant` honest (no write door): stable `sortBy`, `groupBy`/`keyBy` (duplicate keys in `keyBy` refused — last-wins loses a row silently), `chunk`/`take`/`skip`/`slice`, `some`/`every`/`contains`/`flatMap`; the hard half is annotations that hold at PHPStan max with no suppressions, with a method dropped rather than annotated with a lie. ADR-0010 amendment — route: frontier-reasoning / extra
- [ ] [#199](https://github.com/danielPoloWork/egl-util-php/issues/199) **feat: Support\Arr — boundary array toolkit (dot paths, projections, grouping, tabular transforms) — M15 candidate** — filed 2026-08-28 on the maintainer's explicit direction, and the first pass's screening is corrected openly: it was my taste inference, not a recorded decision (the do-NOT-add list never named it). Arrays are the boundary shape before every `fromArray()`; static pure functions, immutable, Lookup's three-way missing-value family reused (`get`/`getOr`/`tryGet`), `keyBy` refuses duplicate keys, `transpose` refuses ragged input — and the honest scope for "matrici" in PHP: tabular transforms yes, linear algebra no (third-party pick row for `markrogoyski/math-php` in the landing PR, the `brick/math` pattern). Boundary-not-domain stated in the ADR so ADR-0008's DTO direction is preserved, not undermined — route: frontier-reasoning / extra
- [x] [#197](https://github.com/danielPoloWork/egl-util-php/issues/197) **feat: Str additions — mask(), truncate(), snakeCase(), camelCase(), isUuid(), isUlid() — M15 candidate** — filed 2026-08-27 from the post-M14 module-coverage review: completes the case family `pascalCase()` opened (the `#[MapFrom]` convention in #188 needs it), adds the validators FR-46's generators shipped without (ULID first-character overflow refused on the read side too, matching refuse-never-truncate), fixed-length PII masking and multibyte truncate. No ADR expected (items 2.2/2.4's precedent); the escalation path is named in the issue if review judges `mask()` security-relevant. **Delivered 2026-08-28** as roadmap item 15.1, spec **r30** (FR-56); 3351 tests (+96). `snakeCase()`/`camelCase()` share one word-splitting engine, which is what makes `camelCase(snakeCase($x)) === $x` provable rather than coincidental for camelCase `$x` — the round trip #188's `#[MapFrom]` needs. `mask()`'s refusal names character counts only, never the value, so a caller logging the failure cannot log the secret — route: fast / medium *(raised from route_advice.py's fast/low floor; session model Sonnet 5 — matched)*
- [ ] [#196](https://github.com/danielPoloWork/egl-util-php/issues/196) **feat: circuit breaker — revisit FR-49's stated non-goal now the CAS store seam exists — M15 candidate** — FR-49 declined it because the library owned no shared cross-call state; item 14.7 then shipped exactly that seam (`RateLimitStore`, CAS, contract-tested twice), so the reason has dissolved — issue #91's own reopening shape, put to the maintainer rather than assumed. The catalogue already carries the candidate bullet; the deliverable is the reversal ADR (three-state breaker, clock-driven cooldown, and the placement question: `Security` beside the limiter vs extracting the store contract to `Support`, since layering forbids `Support → Security`) and then the implementation, opt-in like `Retrier` — route: frontier-reasoning / extra
- [x] [#195](https://github.com/danielPoloWork/egl-util-php/issues/195) **feat: streaming CSV — memory-bounded readLazy()/writeFrom() beside the whole-file API — M15 candidate** — **CLOSED 2026-08-28 as INVALID, the day after filing: the premise is refuted by the source, and the refutation predates the issue.** `Csv::write()` already takes `iterable` and streams through `File::writeStream()` (its own docblock: *"atomically and without buffering the table"*), and `Csv::read()` already returns a Generator (*"a file larger than memory is readable"*) — both proposed methods already exist under the current names, the reserved atomicity decision included. The issue was filed from the public method *names* without reading the docblocks; a surface survey is not a contract survey. The one genuine gap the re-read found (no header→associative mapping on the read side) is #208, filed at the right size — route: frontier-reasoning / extra *(never taken; nothing to route)*
- [ ] [#194](https://github.com/danielPoloWork/egl-util-php/issues/194) **feat: Security\SignedUrl — canonicalized URL signing over Hmac and Url — M15 candidate** — FR-48 shipped the primitive and consumers still hand-roll where signed-URL bugs actually live: canonicalization. Reordered, added or removed query parameters, the signature parameter's own exclusion, path/host/scheme coverage — every wrong answer verifies fine in the happy path. Sign/verify over `Url` values only (one parser in the library), expiry and key rotation ride FR-48/FR-48b unchanged, proxy-rewrite limits stated rather than implied — route: frontier-reasoning / extra
- [ ] [#193](https://github.com/danielPoloWork/egl-util-php/issues/193) **feat: log-context redaction — a RedactingLogger decorator masking secrets before fan-out — M15 candidate** — the logging chain is composable except for the one transform compliance mandates: secrets/PII must not reach the sink, and today a `password` context key or a DSN inside a walked throwable goes to every destination verbatim. ADR-0055's decorator seam, third application: case-insensitive key redaction against a pinned default list (exact-inventory test, ADR-0082's style), a fixed marker never length-preserving, nested arrays and ADR-0029's walked-throwable form covered; free-text message scanning explicitly out of scope because a heuristic must not be sold as a guarantee — route: frontier-reasoning / extra
- [ ] [#192](https://github.com/danielPoloWork/egl-util-php/issues/192) **feat: Support\Xml — XXE-safe XML decoding mirroring Json's posture — M15 candidate** — one door for a format whose PHP surface invites XXE (`LIBXML_NOENT` reads backwards and *enables* substitution): DOCTYPE refused by default, `LIBXML_NONET` always, libxml error state captured and restored, typed `XmlException` naming line/column; the return shape (faithful DOM vs lossy array) is the ADR's main decision. Demand caveat stated honestly in the issue — RFC-0002's estate survey surfaced no XML call sites, so this one is inferred from the enterprise landscape and cheap to defer at the RFC pass — route: frontier-reasoning / extra
- [ ] [#191](https://github.com/danielPoloWork/egl-util-php/issues/191) **feat: Support\Path — traversal-safe path joining and filename sanitization — M15 candidate** — `File` owns *doing* I/O safely; nothing owns *naming* the target, which is where directory traversal lives. Generalizes the defense ADR-0061 built privately (hashed keys so no user byte reaches a path) into public `join()`/`within()`/`sanitizeFilename()` that refuse rather than normalize away `..`, absolute segments, NUL and drive/UNC prefixes — with the honest lexical limit (a symlink can still point out) stated in the ADR instead of implied away — route: frontier-reasoning / extra
- [ ] [#190](https://github.com/danielPoloWork/egl-util-php/issues/190) **feat: Security\Totp — RFC 6238/4226 one-time passwords over the existing Hmac and clock seams — M15 candidate** — the next most hand-rolled security snippet after #92's signed URLs, failing the same ways (`==` comparison, homemade base32, wall-clock reads); every primitive now exists in-house: HMAC discipline, FR-45's injectable clock so vectors pin against `FrozenClock`, the T-03 comparator registry BUG-0001 repaired. Explicit ±N-step window pinned in both directions, an `@internal` RFC 4648 Base32 codec beside `Base64Url`, and the replay limit stated as consumer state rather than papered over — route: frontier-reasoning / extra
- [ ] [#189](https://github.com/danielPoloWork/egl-util-php/issues/189) **feat: Validation group — aggregated, typed input validation upstream of DTO hydration — M15 candidate (needs RFC)** — the intake pipeline is typed end to end except its middle: `Request` refuses shape coercion (ADR-0025), hydration refuses unknown keys and type mismatches (ADR-0008), and nothing owns semantics (lengths, ranges, formats, cross-field rules). Rule value objects plus a `ViolationList` that **aggregates** — deliberately the opposite of hydration's first-throw, because a form needs all violations at once; new deptrac layer over Support only; the `Mail\EmailAddress` layering question (cross-group import is forbidden) named for the RFC rather than duplicated silently. Size L — enters through an RFC/plan pass (RFC-0004 candidate), then one item per PR — route: frontier-reasoning / extra
- [ ] [#188](https://github.com/danielPoloWork/egl-util-php/issues/188) **feat: declarative hydration key mapping — #[MapFrom] attribute (+ optional snake_case convention) — M15 candidate** — hydration matches parameter names exactly, the estate's columns are snake_case, and `RowNormalizer` refuses to touch keys by design (ADR-0042 — "a column name is schema, not data"), so today the choices are schema-shaped DTOs, hand re-keying, or `lenient()` exactly where mass-assignment protection matters. Per-parameter `#[MapFrom]` resolved once into `ClassMetadata`, strict in both directions (mapped-key/param-name collisions and duplicate mappings refused by name), compiled/interpreted parity asserted via `HydrationParityTest`, NFR-01 re-measured — route: frontier-reasoning / extra
- [ ] [#187](https://github.com/danielPoloWork/egl-util-php/issues/187) **feat: DTO serialization — toArray() and JsonSerializable, the missing inverse of hydration — M15 candidate** — `fromArray()` has no inverse anywhere in the `Dto` group (only `Collection::toArray()`, which returns instances), so every envelope, log line and write-back re-implements export by hand and drifts from hydration on enums, nesting and collections. Recursive export symmetric with hydration by contract: backed enums to their backing value, pure enums refused with the property path, and the round-trip property `X::fromArray($x->toArray()) == $x` asserted over the whole T-01 matrix — route: frontier-reasoning / extra
- [x] [#179](https://github.com/danielPoloWork/egl-util-php/issues/179) **security: extend SecretKeyRing rotation to Hmac (v2. grammar, key id under the MAC)** — filed 2026-08-27 as a follow-up from #114 / ADR-0083, deferred on the maintainer's call to keep one security-critical format change per review; **delivered 2026-08-27**, **ADR-0085**, spec **r29** (FR-48b). `Hmac` takes a `SecretKeyRing`; a ring signs as `v2.` = `base64url(keyId ‖ expiry ‖ mac)`. ADR-0083's convention reused unchanged — **the one genuine difference is that HMAC has no AAD, so the key id goes *under* the MAC** (`mac = hmac(keyId ‖ expiry ‖ message)`), which is also what makes a `v2.` body replayed as `v1.` fail: stripping the id leaves a well-formed `v1.` payload the length check cannot refuse. `v1.` stays byte-identical (its conformance vector is the anchor) and a ring verifies `v1.` too, so adoption is a migration. Measured rather than assumed: per message `v2.` ≈ `v1.`, but a `v1.` token from the oldest of three keys costs ~2× — **`v2.` is what keeps verification O(1) during a rotation**. A planted defect also exposed a **pre-existing gap in the `v1.` suite** (the overlong-payload tests could not tell an explicit length check from `hash_equals()` refusing incidentally), closed here — route: frontier-reasoning / high
- [x] [#149](https://github.com/danielPoloWork/egl-util-php/issues/149) **docs: fix endpoint-kernel.md's flagship example and sweep the doc examples nothing has ever run** — ROADMAP 13.3, which never had an issue while its five siblings all did; `new Response()` hits a private constructor, `setHeader()` does not exist, and the static `json()` called through an instance silently drops the `Allow` header RFC 9110 makes mandatory — route: standard / medium
- [x] [#122](https://github.com/danielPoloWork/egl-util-php/issues/122) **release: settle the v1.0.0 tag provenance and correct the gate-approved claim** — The flagship release notes assert the opposite of the repository's own audit trail — route: standard / medium
- [x] [#121](https://github.com/danielPoloWork/egl-util-php/issues/121) **build: register egl/utils on Packagist and verify the tag resolves** — registered + resolution verified 2026-08-10; the `egl/` vendor is squat-protected by that same registration, so the bridge name needs no split repo to defend — route: fast / low
- [x] [#120](https://github.com/danielPoloWork/egl-util-php/issues/120) **release: complete the utils-psr7-bridge publication (split repo, Packagist, first bridge tag)** — **Closed 2026-08-27 as NOT PLANNED, the maintainer's decision — not completed, and not abandoned as stale.** Publishing a bridge means pushing to a generated split repository, which needs a credential in this repository's secrets (`GITHUB_TOKEN` structurally cannot write outside its own — ADR-0033's consequence, not an implementation detail), and the maintainer has decided not to hold one. Both bridges therefore **stay monorepo-only**: specified, contract-tested against two PSR-17 vendors on every PR, usable by anything consuming this repo directly, but never `composer require`-able. The `egl/` namespace is squat-protected by `egl/utils` regardless (#121), so this costs nothing in name protection. **What was finished before the decision, and is evidence rather than outcome:** criterion 2 in #170 (the stale `^0.7` header, plus one pipeline for every bridge and a 15-case gate self-test); and in #183 the runbook's stale `BRIDGE_SPLIT_REPO` corrected to the per-package names, and **release mode — the gate ADR-0035 §2 calls unfakeable, which had never executed — reproduced by hand and green for both packages** (PSR-7 65 tests/202 assertions, PSR-18 28/72, against `egl/utils v1.0.0` from Packagist). So the pipeline is proven up to the push; `bridge-release.yml` keeps its **zero runs** and the split-and-push step stays unproven. The `## [0.1.0]` headings added to anchor a tag were folded back to `[Unreleased]` — a versioned heading means *released*. **Reversible**: a deploy key scoped to one split repo needs a single workflow change — route: standard / medium
- [x] [#119](https://github.com/danielPoloWork/egl-util-php/issues/119) **build: add export-ignore rules and cut a v1.0.1 dist-hygiene patch** — **Closed 2026-08-24 as completed.** Criterion 1 landed in #152 (export-ignore rules); criterion 2 was carried by **`v1.1.0`, not a standalone `v1.0.1`** — M14 was already merged and additive, so the next tag was a minor regardless, and a patch release whose only content is one file would have spent a full release cycle on it. Measured at the tag rather than asserted: `git archive v1.1.0 | tar -t` → **524 files → 121** (686 KB), nothing outside `src/main` but `LICENSE`, `README.md`, `composer.json`. Two things are recorded rather than glossed: `phpdoc.dist.xml` was added in #154 *after* the rules and shipped in `v1.1.0`'s dist — fixed forward in #160 with `tools/dist_gate.py`, which asserts the **archive's contents** instead of trusting either list, since the `.gitattributes` comment's argument that a deny-list cannot rot had the failure direction backwards; and criterion 2's *"through the full release gate this time"* did **not** hold — the tag is unsigned, so `verify-tag` failed and the draft job was skipped. That half is **#115**'s territory, which stays open — route: fast / medium
- [x] [#118](https://github.com/danielPoloWork/egl-util-php/issues/118) **docs: ship the consumer on-ramp (composer require, runnable examples, naming map, full surface)** — ROADMAP 13.2 — unanimous across the review: no path from landing on the repo to a first working call — route: standard / medium
- [x] [#117](https://github.com/danielPoloWork/egl-util-php/issues/117) **docs: repair the five dead relative links in docs/changelog/v1/v1.0.0.md** — Entries rolled from the root changelog without path rebasing; same defect class PR 83 fixed elsewhere — route: fast / low
- [x] [#116](https://github.com/danielPoloWork/egl-util-php/issues/116) **ci: wire a Markdown link checker into CI and a link-rebase step into the changelog roll** — ROADMAP 13.4 — proven non-speculative twice now: two dead links found on master, five more in the v1 changelog — route: standard / medium
- [ ] [#115](https://github.com/danielPoloWork/egl-util-php/issues/115) **security: complete the release signing chain (key setup, pre-push tag guard, attestations)** — **three consecutive releases** failed here (v0.11.0, v1.0.0, v1.1.0); `v1.1.0`'s unsigned tag was deleted 2026-08-27 while working #105, so the repository now carries no dangling failed-release tag. **Criterion 2 done 2026-08-24**: `tools/tag_guard.py` + `.githooks/pre-push` refuse an unsigned or lightweight release tag before the push, with a stated-reason override rather than a hard block. **Criterion 1 is the owner's** (register a signing key). **Criterion 3 done 2026-08-27**, ADR-0084 — and it is worth recording *why it changed*: it was declined on the grounds that attesting would need an attached asset, which would reverse release.md's "no build artifacts are attached" decision. **ADR-0076 (issue #98) reversed that decision itself**, for its own reasons, and `release.yml` has attached a CycloneDX SBOM ever since — so the objection was describing a state of affairs that no longer existed, leaving a shipped, trusted-looking, unverifiable artifact on every Release. `draft-release` now signs an SLSA provenance attestation for that SBOM, fail-closed before the draft exists. **It attests the SBOM, not the source**: the zip Packagist builds from the tag is not produced here and cannot be attested, so the original objection's *first* half stands. The unsigned link is still the tag; a signed tag is what closes it, and that is criterion 1 — route: standard / medium
- [x] [#114](https://github.com/danielPoloWork/egl-util-php/issues/114) **security: add key identifiers to Crypto tokens (SecretKeyRing) for rotation** — Rotating after a suspected compromise currently orphans every outstanding token. **Closed 2026-08-27**, ADR-0083 + spec **r28** (FR-40b). `SecretKeyRing::of($current, ...$previous)` is the window; `new Crypto($ring)` emits `v2.` = base64url(keyId ‖ nonce ‖ ciphertext ‖ tag). **The load-bearing decision is that the key id is GCM's AAD, not merely a prefix** — probed on 8.3.1 first (a different *or empty* AAD returns `false`), so the tag covers the id and both a substituted id and a `v2.`-body-as-`v1.` downgrade are refused, each asserted with a live control. Ids are HKDF-derived not caller-assigned (uninvertible, no registry), colliding ids refused, unknown ids fail closed. **A bare `SecretKey` still emits byte-identical `v1.`**, so this is additive under ADR-0059, and a ring reads `v1.` too — migration, not cutover. No NFR-13 cost (14.79 µs bare vs 60 µs budget; the ring is inside noise). ADR-0082's inventory check, from the previous PR, caught the new `@internal` symbol on its first real use. Hmac's own rotation deferred to a follow-up on the maintainer's call, sharing this convention — route: frontier-reasoning / extra
- [x] [#113](https://github.com/danielPoloWork/egl-util-php/issues/113) **feat: extract a ConnectionInterface from DatabaseConnection (additive MINOR)** — The only I/O boundary without a test seam; landed before consumer suites calcified around workarounds. **Closed 2026-08-25**, ADR-0072: `D4np\Utils\Database\Connection` carries the whole public surface (`pdo()` included — `Repository` builds a `Transaction`, so the type it accepts must be transaction-capable), `DatabaseConnection` implements it, and all five consumers widened. Named `Connection` not `ConnectionInterface`: nine house interfaces, none with the suffix. `Transaction` is now `@template`d over its connection so a consumer's `function (DatabaseConnection $db)` closure keeps type-checking — a docblock-only break Roave cannot see, found by our own suite — route: frontier-reasoning / extra
- [x] [#112](https://github.com/danielPoloWork/egl-util-php/issues/112) **ci: run the BC checker report-only on every PR against v1.0.0** — The release-PR-only rationale expired at 1.0; a mid-cycle break was found far from the diff that caused it. **Closed 2026-08-25**, ADR-0031 annotated: `bc_gate.py --report-only` reports against the **oldest tag of the current MAJOR line** (computed, not the literal `v1.0.0`, so it survives a future v2) on every PR, into the job summary and a warning annotation; the release gate is untouched. It never fails for a finding and always fails for a report it could not read — route: standard / medium
- [x] [#111](https://github.com/danielPoloWork/egl-util-php/issues/111) **build: pin the @internal inventory in consistency_lint.py** — Nothing prevents a future PR from stamping @internal onto a frozen symbol and silently shrinking the contract. **Closed 2026-08-27**, ADR-0082: a tenth `consistency_lint.py` check pins the exact `@internal` inventory — **five symbols today, not the two** ADR-0059 originally named (`Base64Url`, `Uint64`, `Page::__construct()` shipped `@internal` legitimately in the additive MINOR since, and nothing had recorded it). Matched strictly to the tag's own docblock line, not a substring search — `Base64Url.php`'s docblock mentions the literal word twice, once in prose, once as the real tag. Proved failing in both directions by hand against the real tree per the issue's own second criterion, then given a repeatable seven-case offline proof (`verify_internal_inventory.py`) that runs on every PR — route: fast / medium
- [x] [#110](https://github.com/danielPoloWork/egl-util-php/issues/110) **ci: add a real-engine database leg (MySQL and PostgreSQL service containers)** — Every DB/persistence behavioral proof ran exclusively on SQLite; **closed 2026-08-24** by ADR-0071 — `EGL_TEST_DB_DSN` points T-02, T-13 and the gateway/repository/pagination suites at an engine, a `database` CI job re-runs them against MySQL 8.4 and PostgreSQL 16 service containers on the PHP 8.1 floor, and `DialectTest` pins what the three engines do differently. Fail-not-skip is enforced four ways, `--fail-on-skipped` included — route: standard / high
- [x] [#109](https://github.com/danielPoloWork/egl-util-php/issues/109) **ci: add a per-diff coverage gate** — The 90 percent total floor lets an untested addition hide inside the headroom; ADR-0007 admits it openly — route: standard / medium
- [x] [#108](https://github.com/danielPoloWork/egl-util-php/issues/108) **ci: widen mutation testing to Persistence (or a nightly advisory full-tree MSI)** — RowNormalizer/TableGateway are data-mapping, injection-adjacent code with no MSI floor. **Closed 2026-08-26**, ADR-0074 + spec **r23**: took the measuring route, because ADR-0040 records that the spec owns NFR-07's number and the scope is the maintainer's call. A nightly advisory full-tree run plus `tools/mutation_scope_report.py` now reports every namespace from one run. **Measured: Persistence 75.84% and Http 88.89% would both clear a 70% floor today, but a FULL-TREE gate would fail — Mail is at 68.18%.** Adding the two is now a one-line config change plus a spec revision, whenever you want it — route: standard / medium
- [x] [#107](https://github.com/danielPoloWork/egl-util-php/issues/107) **docs: implement the phpDocumentor API-docs build or retract the claim** — ROADMAP 13.7 — a mandatory per-PR gate in AGENTS.md that has never run once — route: standard / medium
- [x] [#106](https://github.com/danielPoloWork/egl-util-php/issues/106) **release: single-source the release notes and generate the Release body from the changelog** — ROADMAP 13.5 plus mechanical generation, so a hand-published Release can never diverge from the record — route: standard / medium
- [x] [#105](https://github.com/danielPoloWork/egl-util-php/issues/105) **release: add a post-publish verification step to the release tooling** — Assert GitHub-side state after a release: tag verified, Release live and non-draft, package visible on Packagist. **Closed 2026-08-27**, ADR-0081: `tools/post_publish_gate.py` added, plus a nightly CI job. **The gap turned out live, not hypothetical**: investigating found `v1.1.0` tagged and pushed but never released — unsigned tag, `verify-tag` correctly refused it, three days with no GitHub Release and nothing on Packagist while ROADMAP.md read as though it had shipped. The broken tag was deleted (documented agent authority for an unpublished tag whose release run visibly failed); re-tagging signed is the maintainer's own next step, since this session holds no signing key. `v1.0.0`, run through the new gate, confirms its two already-documented gaps (unsigned tag, hand-written body — issues #115, #122) rather than false-flagging anything new — route: fast / medium
- [x] [#104](https://github.com/danielPoloWork/egl-util-php/issues/104) **docs: add response-time targets to SECURITY.md** — Reporters need to know when silence means escalate. **Closed 2026-08-27**, ADR-0080: `SECURITY.md` now targets 5 business days to acknowledge and 15 to a triage verdict, sized to solo capacity and explicitly labeled targets rather than a guarantee — narrowing rather than reversing ADR-0060's original "no SLA" stance from the same review board. Escalation depends on nothing the maintainer has to maintain: bump the same thread, and at 30 days of total silence the reporter is released to their own disclosure timeline. `maintenance.md`'s stale "no SLA" sentence corrected to point at ADR-0080 — route: fast / low
- [x] [#103](https://github.com/danielPoloWork/egl-util-php/issues/103) **ci: add a scheduled taint-analysis job (Psalm)** — PHPStan carries security weight here (`literal-string` on SqlStatement) but does no taint tracking. **Closed 2026-08-26**, ADR-0073: a nightly `taint` job runs Psalm `--taint-analysis` outside the 8.1 dependency graph against a triaged two-entry baseline (both `ExceptionHandler`'s debug branch, which taint analysis cannot model). **The finding that mattered:** out of the box Psalm missed a `$_GET` → `composed()` → `PDO::prepare()` flow entirely — taint is lost at SqlStatement's value-object boundary — so `composed()` now carries `@psalm-taint-sink sql $sql`, and the planted flow is caught. Proved the gate can fail, and that a stale baseline entry fails too — route: standard / medium
- [x] [#102](https://github.com/danielPoloWork/egl-util-php/issues/102) **security: harden three minor edges (per-hop scheme re-check, Hash downgrade signal, CSV guard visibility)** — Three documented trade-offs worth tightening; none exploitable as shipped. **Closed 2026-08-26**, ADR-0079 + spec **r27**. **The per-hop scheme check was not added, because it was measured unreachable**: probed with an origin emitting arbitrary `Location` headers, PHP's http wrapper never leaves http/https — `ftp`/`gopher` refused, `file`/`php`/`data`/protocol-relative all degraded to a same-host path — so the check would be inert code (ADR-0022's precedent). T-07 pins all six shapes instead, plus a control proving the payloads are readable. **The real residual is cross-origin following**, now stated on `HttpClient`. `Hash::strict()` added as the discoverable fail-closed construction; `new Hash()` stays permissive under the freeze and is named as a hazard + 2.0 candidate. CSV guard default unchanged per ADR-0037, visibility raised in the docblock and a new README example — route: frontier-reasoning / high
- [x] [#101](https://github.com/danielPoloWork/egl-util-php/issues/101) **test: wire-level mail capture leg (Mailpit) completing T-10** — Header-injection defense is proven at the mail() argument seam, never on a real wire. **Closed 2026-08-26**, ADR-0078 + spec **r26**: a `mail-wire` CI job runs msmtp into a Mailpit sink and `WireCaptureTest` asserts on captured messages — bcc delivered as an envelope recipient, RFC 2047 subjects decoding across 2/3/4-byte **and folded** widths (the folded path had never been sent through a real `mail()`), the envelope sender arriving, and the string header block ADR-0056 rejected really delivering an injected `Bcc`. **Two findings:** Mailpit *rewrites* the stored message by prepending a synthetic `Bcc:` header, so the obvious "no Bcc header survived" assertion fails against a correct pipeline and the right test is its inverse; and **`--fail-on-skipped` does not see a suite skipped from `setUpBeforeClass()`** (exit 0), so ADR-0071's guard would have been inert here — the skip moved into `setUp()` — route: standard / medium
- [x] [#100](https://github.com/danielPoloWork/egl-util-php/issues/100) **test: run one CI cell with randomized test order (logged seed)** — Flush hidden inter-test coupling before the suite grows past 3,000 tests (it already had, at 3,199, by the time this landed). **Closed 2026-08-26**, ADR-0077 + spec **r25**: a fourth `ci.yml` `build` matrix cell (`php-8.3 / random-order`) runs the suite with `--order-by=random`; PHPUnit 10 prints the seed automatically, satisfying reproducibility for free. `docs/development/local-build.md` states the "coupling, not flake" rule the second criterion asked for. First run: all 3,199 tests green, no coupling found — route: fast / low
- [x] [#99](https://github.com/danielPoloWork/egl-util-php/issues/99) **ci: consume the benchmark control-breach exit code with one automatic re-run** — bench_regression_gate.py already distinguished invalid run (exit 2) from regression, but CI treated both as failure. **Closed 2026-08-25**, ADR-0057 annotated: on exit 2 the benchmark job re-measures HEAD and the base commit together (not just one half — ADR-0030's same-runner argument needs the two halves adjacent) and re-runs the identical gate call exactly once; a second invalid run fails loudly. Exit 1 is never retried, on either attempt — route: fast / medium
- [x] [#98](https://github.com/danielPoloWork/egl-util-php/issues/98) **build: supply-chain hygiene batch (scheduled audit, require-checker, lock cadence, SBOM)** — Four small Build-seat recommendations, one PR-sized batch. **Closed 2026-08-26**, ADR-0076 + spec **r24**: `composer audit` now also runs nightly; ComposerRequireChecker joins `ci.yml`'s hygiene job outside the dependency graph, and its **first real run found two genuinely undeclared hard dependencies** — `ext-filter` and `ext-session`, now in `require` (NFR-08 amended) — while `ext-openssl`/`ext-intl` were already guarded and joined `suggest` instead; a CycloneDX SBOM (production deps only) now attaches to every draft Release. The fourth item (lock refresh cadence) was already met by the existing weekly `dependabot.yml` composer config — route: fast / medium
- [x] [#97](https://github.com/danielPoloWork/egl-util-php/issues/97) **feat: PSR-20 clock in Support (SystemClock / FrozenClock) — M14 candidate** — delivered as **M14 item 14.1** (spec r17 FR-45, ADR-0062): `psr/clock` is the third interface-only require, and Support gained its first outward deptrac edge, proven discriminating — route: frontier-reasoning / extra
- [x] [#96](https://github.com/danielPoloWork/egl-util-php/issues/96) **feat: sortable identifiers — Str::ulid() and Str::uuidV7() — M14 candidate** — delivered as **M14 item 14.2** (spec r18 FR-46/NFR-15, ADR-0063): the out-of-scope monotonicity is pinned as a mechanism, since no behavioural test can see it — route: frontier-reasoning / extra
- [x] [#95](https://github.com/danielPoloWork/egl-util-php/issues/95) **feat: pagination value objects in Persistence — M14 candidate** — delivered as **M14 item 14.3** (spec r19 FR-47, ADR-0064): an unordered query is refused, since unstable pagination is a correctness bug with no symptom; the count is a builder clause so `composed()` stays unspent — route: frontier-reasoning / extra
- [x] [#94](https://github.com/danielPoloWork/egl-util-php/issues/94) **feat: retry/backoff policy object in Support — M15 candidate** — **M14 item 14.5** (FR-49) — depends on 14.1; wall-clock deadline required, not just attempt count — route: frontier-reasoning / extra
- [x] [#93](https://github.com/danielPoloWork/egl-util-php/issues/93) **feat: utils-psr18-bridge sub-package (PSR-18 adapter over HttpClient)** — **Closed 2026-08-26**, ADR-0075 + spec **03**. The RFC-0003 deferral was **overridden by the maintainer** on shown evidence that its revisit condition is still unmet (#120 open, `bridge-release.yml` has zero runs, no split repo, Packagist 404) — recorded in the ADR rather than quietly bypassed. `Psr18Client` adapts `HttpClient` to `ClientInterface`; **it does not depend on the PSR-7 bridge** (that converts the *server* vocabulary, this wraps the *client* — different types, no shared code). PSR-18's request/network exception split is made **structurally** (every request-shaped check runs before the send) rather than by matching message text. One publication pipeline now serves every bridge: the tag names its package, and `bridge_release_gate.py` gained a 15-case self-test it never had — route: frontier-reasoning / extra
- [x] [#92](https://github.com/danielPoloWork/egl-util-php/issues/92) **feat: HMAC signing utility (signed URLs, webhook signatures) — M14/M15 candidate** — **M14 item 14.4** (FR-48) — does NOT block on #114: ADR-0054's versioned prefix absorbs key ids as v2 — route: frontier-reasoning / extra
- [x] [#91](https://github.com/danielPoloWork/egl-util-php/issues/91) **feat: rate-limiting primitive (token bucket / fixed window) — M15 candidate** — **reopened 2026-08-13** (the deferral's revisit condition was unreachable — the seam it waited for is this issue's own deliverable); design settled in ADR-0061 as **M14 item 14.6**, implementation is **14.7** after 14.1's clock — route: frontier-reasoning / extra
- [x] [#90](https://github.com/danielPoloWork/egl-util-php/issues/90) **docs: publish a third-party picks pattern page (brick/math, symfony/cache, symfony/mailer)** — published, linked from README.md and the patterns catalogue index — route: fast / medium
- [x] [#89](https://github.com/danielPoloWork/egl-util-php/issues/89) **docs: publish docs/upgrading.md, the consumer-facing deprecation and support guidance** — published, linked from README.md and SECURITY.md; corrected a post-1.0 removal-timing error found while writing the example — route: fast / medium
- [x] [#88](https://github.com/danielPoloWork/egl-util-php/issues/88) **docs: prepend a consumer highlights section to each per-version changelog** — Highlights section added to v1.0.0.md; release.md's roll step gained the matching instruction — route: fast / low
- [x] [#87](https://github.com/danielPoloWork/egl-util-php/issues/87) **docs: add the community files (CONTRIBUTING, CODE_OF_CONDUCT, bridge LICENSE)** — CONTRIBUTING built from existing gates, Contributor Covenant 2.1 CoC, bridge LICENSE added — route: fast / low
- [x] [#86](https://github.com/danielPoloWork/egl-util-php/issues/86) **chore: apply the GitHub-side configuration (type labels, milestones)** — naming convention reconciled to vX.Y.Z (matching all 14 ROADMAP headers, AGENTS.md §6.4 corrected); 11 labels applied, 12 stale milestones closed, `post-1.0`/`v1.1.0` created; 39-issue batch labelled — route: fast / low
- [x] [#85](https://github.com/danielPoloWork/egl-util-php/issues/85) **docs: sweep the review board minor findings (template artifacts, stale comments, small gaps)** — nine of ten items fixed by [PR #131](https://github.com/danielPoloWork/egl-util-php/pull/131). **Closed 2026-08-26**: the tenth (an author email in `composer.json`/the bridge's `composer.json`) is the maintainer's explicit decision to omit — a personal address in a public Composer package is a publication choice, not a defect — so the checklist item is resolved by that decision rather than by a further edit — route: fast / medium
- [x] [#84](https://github.com/danielPoloWork/egl-util-php/issues/84) **chore: define Milestone 14, the post-1.0 functional roadmap** — RFC-0003 accepted, M14 written with five items; #91 and #93 deferred with reasons and left open — route: standard / high
